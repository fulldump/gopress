<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>GoPress · Error {{ .error.Status }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex,nofollow">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/basic.css?v=4">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">{{ .headTrailCode }}
</head>
<body>
    <header class="site-header">
        <div class="container">
            <div class="brand">
                <a class="brand-logo" href="/">GoPress</a>
                <p class="brand-tagline">Ideas rigurosas contadas con elegancia</p>
            </div>
            <nav class="session" aria-label="Área personal">
                <div id="login" class="session-login">
                    <a class="button subtle" href="/auth/login">Iniciar sesión</a>
                </div>
                <div id="logout" class="session-user" hidden>
                    <img id="user_picture" src="" alt="Avatar" class="session-avatar">
                    <div class="session-meta">
                        <span id="user_nick" class="session-name"></span>
                        <div class="session-links">
                            <a href="/admin/">Administración</a>
                            <span aria-hidden="true">·</span>
                            <a href="/auth/logout">Salir</a>
                        </div>
                    </div>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <section class="hero hero--compact">
            <div class="container">
                <p class="hero-kicker">Algo no salió como esperábamos</p>
                <h1 class="hero-title">Error {{ .error.Status }} · {{ .error.Title }}</h1>
                <p class="hero-subtitle">{{ .error.Description }}</p>
            </div>
        </section>
    </main>

    <footer class="site-footer">
        <div class="container">
            <p class="footer-heading">¿Necesitas ayuda?</p>
            <p>Vuelve al <a href="/">inicio de GoPress</a> o contáctanos a través de <a href="https://github.com/fulldump/gopress">GitHub</a>.</p>
        </div>
    </footer>

    <script>
        const login = document.getElementById('login');
        const logout = document.getElementById('logout');
        const picture = document.getElementById('user_picture');
        const nick = document.getElementById('user_nick');

        fetch('/auth/me')
            .then(resp => resp.json())
            .then(me => {
                if (me.error) return;
                login.hidden = true;
                logout.hidden = false;
                logout.style.display = 'flex';
                picture.src = me.picture;
                nick.textContent = me.nick;
            })
            .catch(() => {});
    </script>
</body>
</html>
