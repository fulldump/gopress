<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>{{ .article.Title }} · GoPress</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="{{ .og_description }}">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/basic.css?v=4">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">

    <meta property="og:title" content="{{ .og_title }}" />
    <meta property="og:url" content="{{ .og_url }}" />
    <meta property="og:image" content="{{ .og_image }}" />
    <meta property="og:site_name" content="GoPress, blogs para todos" />
    <meta property="og:description" content="{{ .og_description }}" />
    <meta name="twitter:card" content="summary" />{{ .headTrailCode }}
</head>
<body>
    <header class="site-header">
        <div class="container">
            <div class="brand">
                <a class="brand-logo" href="/">GoPress</a>
                <p class="brand-tagline">Ideas rigurosas contadas con elegancia</p>
            </div>
            <nav class="session" aria-label="Área personal">
                <div id="login" class="session-login">
                    <a class="button subtle" href="/auth/login">Iniciar sesión</a>
                </div>
                <div id="logout" class="session-user" hidden>
                    <img id="user_picture" src="" alt="Avatar" class="session-avatar">
                    <div class="session-meta">
                        <span id="user_nick" class="session-name"></span>
                        <div class="session-links">
                            <a href="/admin/">Administración</a>
                            <span aria-hidden="true">·</span>
                            <a href="/auth/logout">Salir</a>
                        </div>
                    </div>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <article class="full-article">
            <header class="full-article__hero">
                <div class="container">
                    <div class="article-meta">
                        {{ if .article.AuthorPicture }}
                        <a class="article-meta__avatar" href="/user/{{ .article.AuthorNick }}">
                            <img class="avatar" src="{{ .article.AuthorPicture }}" alt="Avatar de {{ .article.AuthorNick }}">
                        </a>
                        {{ end }}
                        <div class="article-meta__info">
                            <a class="article-meta__author" href="/user/{{ .article.AuthorNick }}">{{ .article.AuthorNick }}</a>
                            <div class="article-meta__details">
                                <time datetime="{{ .article.PublishOn.Format "2006-01-02" }}">{{ .article.PublishOn.Format "2 de January de 2006" }}</time>
                                <span aria-hidden="true">·</span>
                                <span>{{ .article.Stats.Views }} lecturas</span>
                            </div>
                        </div>
                    </div>
                    <h1 class="full-article__title">{{ .article.Title }}</h1>
                    {{ if .article.Tags }}
                    <div class="tag-list" aria-label="Etiquetas del artículo">
                        {{ range $j, $tag := .article.Tags }}
                            <a class="tag" href="/user/{{ $.article.AuthorNick }}/tag/{{ $tag }}">{{ $tag }}</a>
                        {{ end }}
                    </div>
                    {{ end }}
                </div>
            </header>

            <div class="container">
                <div class="full-article__content">
                    {{ .article.ContentHTML }}
                </div>
            </div>
        </article>
    </main>

    <footer class="site-footer">
        <div class="container">
            <p class="footer-heading">¿Te gustaría escribir aquí?</p>
            <p>GoPress es software libre. Crea tu cuenta y comparte tus propias historias o contribuye en <a href="https://github.com/fulldump/gopress">GitHub</a>.</p>
            <p>Comparte la conversación también en <a href="https://www.twitch.tv/fulldump">Twitch</a> y <a href="https://www.youtube.com/watch?v=xweWe4ve8vE&list=PLMPsqnO6mKSA3iQzqaRIi_1qVONlQN8ap&index=1">YouTube</a>.</p>
        </div>
    </footer>

    <script>
        const login = document.getElementById('login');
        const logout = document.getElementById('logout');
        const picture = document.getElementById('user_picture');
        const nick = document.getElementById('user_nick');

        fetch('/auth/me')
            .then(resp => resp.json())
            .then(me => {
                if (me.error) return;
                login.hidden = true;
                logout.hidden = false;
                logout.style.display = 'flex';
                picture.src = me.picture;
                nick.textContent = me.nick;
            })
            .catch(() => {});
    </script>
</body>
</html>
