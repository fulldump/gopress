<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>GoPress.org</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Historias y conocimiento creado por la comunidad de GoPress.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Playfair+Display:wght@500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/basic.css?v=4">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">{{ .headTrailCode }}
</head>
<body>
    <header class="site-header">
        <div class="container">
            <div class="brand">
                <a class="brand-logo" href="/">GoPress</a>
                <p class="brand-tagline">Ideas rigurosas contadas con elegancia</p>
            </div>
            <nav class="session" aria-label="Área personal">
                <div id="login" class="session-login">
                    <a class="button subtle" href="/auth/login">Iniciar sesión</a>
                </div>
                <div id="logout" class="session-user" hidden>
                    <img id="user_picture" src="" alt="Avatar" class="session-avatar">
                    <div class="session-meta">
                        <span id="user_nick" class="session-name"></span>
                        <div class="session-links">
                            <a href="/admin/">Administración</a>
                            <span aria-hidden="true">·</span>
                            <a href="/auth/logout">Salir</a>
                        </div>
                    </div>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <section class="hero">
            <div class="container">
                <p class="hero-kicker">Blog colectivo de tecnología y creatividad</p>
                <h1 class="hero-title">Lecturas que inspiran a pensar, escribir y crear</h1>
                <p class="hero-subtitle">Explora las últimas publicaciones de la comunidad, seleccionadas para un ritmo de lectura sereno y con foco en la calidad.</p>
            </div>
        </section>

        <section class="article-feed container" aria-label="Artículos recientes">
            {{ if .articles }}
                {{ range $i, $article := .articles }}
                <article class="article-card">
                    <header class="article-card__header">
                        {{ if .AuthorPicture }}
                            <a class="article-card__author" href="/user/{{ .AuthorNick }}">
                                <img class="avatar" src="{{ .AuthorPicture }}" alt="Avatar de {{ .AuthorNick }}">
                            </a>
                        {{ end }}
                        <div class="article-card__meta">
                            <a class="article-card__author-name" href="/user/{{ .AuthorNick }}">{{ .AuthorNick }}</a>
                            <div class="article-card__details">
                                <time datetime="{{ .PublishOn.Format "2006-01-02" }}">{{ .PublishOn.Format "2 de January de 2006" }}</time>
                                <span aria-hidden="true">·</span>
                                <span>{{ .Stats.Views }} lecturas</span>
                            </div>
                        </div>
                    </header>
                    <div class="article-card__body">
                        <h2 class="article-card__title">
                            <a href="/user/{{ .AuthorNick }}/article/{{ .Url }}">{{ .Title }}</a>
                        </h2>
                        <div class="article-card__summary">
                            {{ .ContentSummary }}
                        </div>
                    </div>
                    <footer class="article-card__footer">
                        <a class="read-more" href="/user/{{ .AuthorNick }}/article/{{ .Url }}">Seguir leyendo</a>
                    </footer>
                </article>
                {{ end }}
            {{ else }}
                <p class="empty-state">Todavía no hay artículos publicados. Sé la primera persona en compartir una historia.</p>
            {{ end }}
        </section>
    </main>

    <footer class="site-footer">
        <div class="container">
            <p class="footer-heading">GoPress es software libre creado con cariño.</p>
            <p>Colabora y aporta ideas en <a href="https://github.com/fulldump/gopress">GitHub</a>.</p>
            <p>También puedes acompañar a la comunidad en <a href="https://www.twitch.tv/fulldump">Twitch</a> y <a href="https://www.youtube.com/watch?v=xweWe4ve8vE&list=PLMPsqnO6mKSA3iQzqaRIi_1qVONlQN8ap&index=1">YouTube</a>.</p>
        </div>
    </footer>

    <script>
        const login = document.getElementById('login');
        const logout = document.getElementById('logout');
        const picture = document.getElementById('user_picture');
        const nick = document.getElementById('user_nick');

        fetch('/auth/me')
            .then(resp => resp.json())
            .then(me => {
                if (me.error) return;
                login.hidden = true;
                logout.hidden = false;
                logout.style.display = 'flex';
                picture.src = me.picture;
                nick.textContent = me.nick;
            })
            .catch(() => {});
    </script>
</body>
</html>
