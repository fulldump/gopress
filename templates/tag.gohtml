<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>Etiqueta {{ .tag }} · GoPress</title>
    <meta name="robots" content="noindex">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/styles/basic.css?v=5">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">{{ .headTrailCode }}
</head>
<body>
    <header class="site-header">
        <div class="container header-inner">
            <div class="site-branding">
                <a class="site-title" href="/">GoPress.org</a>
                <p class="site-tagline">Explora los temas que la comunidad conversa hoy.</p>
            </div>
            <nav class="site-actions" aria-label="Acciones de la cuenta">
                <div id="login">
                    <a class="button subtle" href="/auth/login">Iniciar sesión</a>
                </div>
                <div id="logout" class="user-actions" style="display:none;">
                    <img id="user_picture" src="" alt="Avatar del usuario">
                    <div class="user-info">
                        <span id="user_nick" class="user-nick"></span>
                        <div class="links">
                            <a href="/admin/">Administración</a>
                            <span aria-hidden="true">·</span>
                            <a href="/auth/logout">Salir</a>
                        </div>
                    </div>
                </div>
            </nav>
        </div>
    </header>

    <main class="container main-content">
        <section class="hero" aria-labelledby="tag-title">
            <h1 id="tag-title">#{{ .tag }}</h1>
            <p>Un recorrido por las publicaciones que hablan sobre <strong>{{ .tag }}</strong>. Inspírate con diferentes voces y perspectivas.</p>
        </section>

        <section class="articles-grid" aria-label="Artículos relacionados">
            {{ range $i, $article := .articles }}
            <article class="article-card">
                <header class="card-header">
                    {{ if $article.AuthorPicture }}
                    <a class="author-avatar" href="/user/{{ $article.AuthorNick }}">
                        <img src="{{ $article.AuthorPicture }}" alt="Foto de {{ $article.AuthorNick }}">
                    </a>
                    {{ end }}
                    <div class="card-meta">
                        <a class="author-name" href="/user/{{ $article.AuthorNick }}">@{{ $article.AuthorNick }}</a>
                        <time datetime="{{ $article.PublishOn.Format "2006-01-02" }}">{{ $article.PublishOn.Format "02 Jan 2006" }}</time>
                        <span class="meta-item">{{ $article.Stats.Views }} lecturas</span>
                    </div>
                </header>
                <h2>
                    <a href="/user/{{ $article.AuthorNick }}/article/{{ $article.Url }}">{{ $article.Title }}</a>
                </h2>
                <div class="article-excerpt content">
                    {{ $article.ContentSummary }}
                </div>
                {{ if $article.Tags }}
                <ul class="tag-list" aria-label="Etiquetas del artículo">
                    {{ range $tag := $article.Tags }}
                    <li><a class="tag-chip" href="/user/{{ $article.AuthorNick }}/tag/{{ $tag }}">{{ $tag }}</a></li>
                    {{ end }}
                </ul>
                {{ end }}
                <footer class="card-footer">
                    <span>Publicado por <a href="/user/{{ $article.AuthorNick }}">{{ $article.AuthorNick }}</a></span>
                    <a class="read-more" href="/user/{{ $article.AuthorNick }}/article/{{ $article.Url }}">Seguir leyendo</a>
                </footer>
            </article>
            {{ end }}
        </section>
    </main>

    <footer class="footer">
        <div class="container footer-inner">
            <p>GoPress es software libre hecho con mucho ❤️. Colabora y comparte tus mejoras.</p>
            <div class="icon-links">
                <a href="https://github.com/fulldump/gopress">
                    <img class="icon" src="https://github.githubassets.com/favicons/favicon.png" alt="GitHub">
                    Código en GitHub
                </a>
                <a href="https://www.twitch.tv/fulldump">
                    <img class="icon" src="https://static.twitchcdn.net/assets/favicon-32-e29e246c157142c94346.png" alt="Twitch">
                    twitch.tv/fulldump
                </a>
                <a href="https://www.youtube.com/watch?v=xweWe4ve8vE&list=PLMPsqnO6mKSA3iQzqaRIi_1qVONlQN8ap&index=1">
                    <img class="icon" src="https://www.youtube.com/s/desktop/286e6262/img/favicon_32x32.png" alt="YouTube">
                    youtube.com/@fulldump
                </a>
            </div>
        </div>
    </footer>

    <script>
        (function () {
            const login = document.getElementById('login');
            const logout = document.getElementById('logout');
            const picture = document.getElementById('user_picture');
            const nick = document.getElementById('user_nick');

            fetch('/auth/me')
                .then(function (resp) { return resp.ok ? resp.json() : {}; })
                .then(function (me) {
                    if (!me || me.error) return;
                    if (login) login.style.display = 'none';
                    if (logout) logout.style.display = 'flex';
                    if (picture && me.picture) picture.setAttribute('src', me.picture);
                    if (nick) nick.textContent = me.nick;
                })
                .catch(function () { /* noop */ });
        })();
    </script>
</body>
</html>
